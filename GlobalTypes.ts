export interface FormFields {
    email: String
    password: String
}

export interface FirstTemplate {
    id?:string,
    FullName:string,
    Email:string,
    Subject:string,
    Phone:string
    Message:string
}